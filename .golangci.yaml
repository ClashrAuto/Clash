linters:
  disable-all: true
  enable:
    - gofumpt
    - staticcheck
    - govet
    - gci

linters-settings:
  gci:
    sections:
      - standard
      - prefix(github.com/ClashrAuto/clash)
      - default
  staticcheck:
    go: '1.18'
